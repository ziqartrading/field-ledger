# Field Ledger public frontend v3.2.0

Fast login and Google Apps Script bridge repair. The matching private Apps Script backend v3.2.0 must be deployed first.

# Field Ledger public client v3.2.0

This repository contains only the public PWA client. Business data, passwords, Apps Script source, Google Drive IDs and backups must never be committed here.

Version 3.2.0 fixes all Admin user-management entry points, adds a dedicated tablet setup panel, install-to-home-screen support, a keep-awake option for long syncs, and best-effort Background Sync retry signalling.

GitHub Pages: branch `main`, folder `/docs`.
